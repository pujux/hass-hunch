{
  "config": {
    "step": { "user": { "title": "Connect Hunch to TypeSafe", "data": { "api_key": "TypeSafe API key" } } },
    "error": {
      "invalid_auth": "The API key was rejected.",
      "unknown_model": "The pinned Jev model does not exist.",
      "cannot_connect": "Could not reach the TypeSafe API."
    },
    "abort": { "already_configured": "Hunch is already set up." }
  },
  "options": {
    "step": {
      "init": {
        "title": "Hunch options",
        "data": {
          "fallback_agent": "Fallback conversation agent",
          "timer_script": "Script to run when a timer finishes (gets name, duration_text, label, description, duration_seconds, device_id, area_id and more as variables; see the README)",
          "model": "Jev model",
          "response_language": "Response language",
          "timeout_ms": "Jev timeout (ms)",
          "max_silent_targets": "Max silent targets",
          "device_round": "Extra Jev round over device names when too many candidates",
          "max_rounds": "Max clarification rounds",
          "threshold_verb_fire": "Verb-fire confidence (default 0.7)",
          "threshold_scope_fire": "Scope-fire confidence (default 0.7)",
          "threshold_place_override": "Place-override confidence (default 0.9)",
          "threshold_collective": "Collective confidence (default 0.5)",
          "threshold_target_choice_conf": "Target-choice confidence (default 0.7)",
          "threshold_auto_execute": "Auto-execute confidence (default 0.7)",
          "threshold_confirm_band": "Confirm-band confidence (default 0.5)",
          "threshold_flag": "Flag confidence (default 0.6)",
          "threshold_specific_device": "Specific-device confidence (default 0.7)",
          "threshold_collective_fallback": "Collective-fallback confidence (default 0.4)",
          "threshold_no_match_clarify": "No-match clarify confidence (default 0.6)"
        }
      }
    },
    "error": {
      "cannot_select_self": "Hunch cannot be its own fallback."
    }
  },
  "selector": {
    "response_language": {
      "options": {
        "auto": "Same as the request",
        "en": "English",
        "de": "German"
      }
    }
  }
}
